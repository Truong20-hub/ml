[project]
name = "HocMayCoBan"
version = "0.1.0"
description = "Machine learning gateway for local-to-Colab prediction service"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.30.0",
  "pydantic>=2.7.0",
  "pydantic-settings>=2.2.1",
  "requests>=2.32.0",
  "python-dotenv>=1.0.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
